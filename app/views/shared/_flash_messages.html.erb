<% [:success, :notice, :alert, :error].each do |level| %>
  <% if flash[level].present? %>
  		<div class="alert <%# flash_class(level) %>"> 
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= flash[level] %>
    </div>
  <% end %>
<% end %>