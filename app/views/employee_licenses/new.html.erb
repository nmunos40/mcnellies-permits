<h1>Add New License</h1>

<%= form_for @employee_license do |f| %>
	<%= render 'fields', f: f %>
	<div class="form-actions">
		<%= f.submit "Add License", class: "btn btn-success" %>
		<%= link_to 'Cancel', employee_licenses_path, class: "btn btn-link" %>
	</div>
<% end %>