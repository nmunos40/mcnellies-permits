<h1>Editing license</h1>

<%= form_for @license do |f| %>
	<%= render 'fields', f: f %>
	<div class="form-actions">
		<%= f.submit "Create License", class: "btn btn-success" %>
		<%= link_to "Cancel", license_path, class: "btn btn-link" %>
	</div>
<% end %>