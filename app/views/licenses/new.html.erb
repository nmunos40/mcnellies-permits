<h4>Create New License</h4>

<%= form_for @license do |f| %>
	<%= render 'fields', f: f %>
	<div class="form-actions">
		<%= f.submit "Create License", class: "btn btn-success" %>
		<%= link_to "Cancel", sites_path, class: "btn btn-link" %>
	</div>
<% end %>